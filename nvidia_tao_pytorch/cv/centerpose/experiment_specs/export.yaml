encryption_key: "???"
results_dir: "???"

export:
  gpu_id: 0
  checkpoint: "???"
  onnx_file: "???"
  input_channel: 3
  input_width: 512
  input_height: 512
  opset_version: 16
  do_constant_folding: True

model:
  down_ratio: 4
  backbone:
    model_type: "???"